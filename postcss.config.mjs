// ============================================================
// PostCSS xử lý CSS trước khi gửi ra trình duyệt.
// Tailwind v4 dùng plugin "@tailwindcss/postcss" (khác với v3).
// ============================================================
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
